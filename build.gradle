apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'com.demo', name: 'dropwizard-core', version: '1.0.0'
}

apply plugin: 'application'

mainClassName = 'com.rest.App'

run {
    args 'server'
}